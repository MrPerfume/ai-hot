+++
title = "AI 写代码越来越强，但它不知道我们上周修了什么 bug，把 Bug 重新引入生产环境了"
description = "三个月前，我们花了两周时间修了一个 bug 。 用户账单偶尔会出现 ¥0.01 的误差。复现不稳定，线上偶发，客服一直在手动补差价。翻遍了日志，最后定位到一个经典问题：浮点数精度。 `0.1 + 0.2` 在 IEEE 754 里不等于 `"
seo_title = "AI 写代码越来越强，但它不知道我们上周修了什么 bug，把 Bug 重新引入生产环境了｜AI资讯解读 - AI热榜"
seo_description = "三个月前，我们花了两周时间修了一个 bug 。 用户账单偶尔会出现 ¥0.01 的误差。复现不稳定，线上偶发，客服一直在手动补差价。翻遍了日志，最后定位到一个经典问题：浮点数精度。 `0.1 + 0.2` 在 IEEE 754 里不等于 `"
seo_keywords = "AI 写代码越来越强，但它不知道我们上周修了什么 bug，把 Bug 重新引入生产环境了, V2EX, AI新闻, AI资讯, AI热榜"
slug = "v2ex-1211151"
type = "news"

[params]
id = "v2ex-1211151"
name = "AI 写代码越来越强，但它不知道我们上周修了什么 bug，把 Bug 重新引入生产环境了"
title_en = "AI 写代码越来越强，但它不知道我们上周修了什么 bug，把 Bug 重新引入生产环境了"
original_url = "https://www.v2ex.com/t/1211151"
source = "V2EX"
published = "2026-05-08T05:26:38"
lang = "zh"
intro = "三个月前，我们花了两周时间修了一个 bug 。 用户账单偶尔会出现 ¥0.01 的误差。复现不稳定，线上偶发，客服一直在手动补差价。翻遍了日志，最后定位到一个经典问题：浮点数精度。 `0.1 + 0.2` 在 IEEE 754 里不等于 `0.3`，这个大家都听说过，但真的踩到还是很痛。 修复方案是把所有金额改成以「分」为单位存整数，展示时再除以 100 。改动不复杂，但涉及的地方很多，改完之后代码里到处是 `amount * 100`、`Math.round()`、`/ 100`，看起来有点丑，但能用，bug 消失了。 然后上个月，我让 Agent 帮我重构结算模块，优化一下代码结构。 它看"
ai_summary = ""
summary = "三个月前，我们花了两周时间修了一个 bug 。 用户账单偶尔会出现 ¥0.01 的误差。复现不稳定，线上偶发，客服一直在手动补差价。翻遍了日志，最后定位到一个经典问题：浮点数精度。 `0.1 + 0.2` 在 IEEE 754 里不等于 `0.3`，这个大家都听说过，但真的踩到还是很痛。 修复方案是把所有金额改成以「分」为单位存整数，展示时再除以 100 。改动不复杂，但涉及的地方很多，改完之后代码里到处是 `amount * 100`、`Math.round()`、`/ 100`，看起来有点丑，但能用，bug 消失了。 然后上个月，我让 Agent 帮我重构结算模块，优化一下代码结构。 它看"
summary_zh = ""
tags = []
list_page = 13
+++

<!-- AUTO-GENERATED: news page -->

三个月前，我们花了两周时间修了一个 bug 。

用户账单偶尔会出现 ¥0.01 的误差。

复现不稳定，线上偶发，客服一直在手动补差价。

翻遍了日志，最后定位到一个经典问题：浮点数精度。

0.1 + 0.2
在 IEEE 754 里不等于 0.3
，这个大家都听说过，但真的踩到还是很痛。

修复方案是把所有金额改成以「分」为单位存整数，展示时再除以 100 。

改动不复杂，但涉及的地方很多，改完之后代码里到处是 amount * 100
、Math.round()
、/ 100
，看起来有点丑，但能用，bug 消失了。

然后上个月，我让 Agent 帮我重构结算模块，优化一下代码结构。

## 🔗 原始来源

如果你要核对细节，可以再看原文：
[V2EX原文链接](https://www.v2ex.com/t/1211151)

